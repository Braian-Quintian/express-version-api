{
  "compilerOptions": {
    "target": "es6", // Versión de ECMAScript a la que se compilará el código
    "module": "commonjs", // Tipo de módulo a generar (CommonJS, ESNext, etc.)
    "outDir": "dist", // Directorio de salida para los archivos compilados
    "strict": true, // Habilitar modo estricto
    "esModuleInterop": true, // Habilitar interoperabilidad de ES modules
    "skipLibCheck": true, // Omitir comprobación de librerías
    "declaration": true, // Generar archivos de definición (.d.ts)
    "declarationDir": "dist", // Directorio para archivos de definición
  },
  "include": ["src/**/*.ts"], // Patrón de archivos a incluir en la compilación
  "exclude": ["node_modules"] // Patrón de archivos a excluir de la compilación
}